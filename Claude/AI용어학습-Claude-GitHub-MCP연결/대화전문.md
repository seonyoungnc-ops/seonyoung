# 대화 전문: AI 용어 학습 및 Claude <-> GitHub MCP 연결

**날짜:** 2026-03-27  
**레포지토리:** seonyoungnc-ops/seonyoung  

---

## 대화 내용

### 1. GitHub 이슈 생성 요청

**사용자:**
> seonyoungnc-ops/seonyoung 레포에 GitHub 이슈 만들어줘
> 
> 제목: AI 용어 학습 및 Claude <-> GitHub MCP 연결  
> 내용: 테스트

**Claude:**  
github:create_issue 툴을 사용하여 이슈를 생성했습니다.

- **이슈 제목:** AI 용어 학습 및 Claude <-> GitHub MCP 연결
- **이슈 번호:** #1
- **링크:** https://github.com/seonyoungnc-ops/seonyoung/issues/1
- **내용:** 테스트
- **상태:** Open

Claude ↔ GitHub MCP 연결이 정상적으로 동작하는 것이 확인되었습니다.

---

### 2. 대화 내용 레포 업로드 요청

**사용자:**
> 지금 이 대화 내용을 레포 내 Claude 폴더를 생성한 뒤, 제목: AI 용어 학습 및 Claude <-> GitHub MCP 연결 으로 올려줘. 대화 내용 확인이 전체 가능해야하고, Claude Desktop에서 깃허브 MCP 연결하는법 이미지로 만들어서 함께 첨부해줘

**Claude:**  
1. Claude Desktop에서 GitHub MCP 연결하는 법을 SVG 이미지로 제작했습니다.
2. `Claude/AI용어학습-Claude-GitHub-MCP연결/` 폴더를 생성하여 아래 파일들을 Push했습니다:
   - `README.md` — 폴더 안내 문서
   - `대화전문.md` — 이 파일 (대화 전체 내용)
   - `claude-desktop-github-mcp-guide.svg` — GitHub MCP 연결 가이드 이미지

---

## Claude Desktop GitHub MCP 연결 방법 요약

### Step 1. GitHub Personal Access Token 발급

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. **Generate new token** 클릭
3. 필요한 권한 선택:
   - `repo` (전체)
   - `workflow`
   - `read:org`
4. 생성된 토큰(`ghp_...`) 복사 후 안전한 곳에 보관

### Step 2. Claude Desktop 설정 파일 열기

- **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

### Step 3. 설정 파일에 MCP 서버 추가

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_YOUR_TOKEN_HERE"
      }
    }
  }
}
```

> ⚠️ `ghp_YOUR_TOKEN_HERE` 자리에 Step 1에서 발급받은 실제 토큰을 넣으세요.

### Step 4. Claude Desktop 재시작 후 확인

1. Claude Desktop 완전히 종료 후 재시작
2. 채팅창 우하단에 🔧 MCP 아이콘이 표시되면 연결 성공
3. Claude에게 `"seonyoungnc-ops/seonyoung 레포 이슈 목록 보여줘"` 같이 요청해서 동작 확인

> **사전 조건:** Node.js가 설치되어 있어야 합니다. 터미널에서 `node -v`로 확인 가능.

---

## 결과 및 확인 사항

- ✅ GitHub MCP 연결 정상 동작 확인
- ✅ Claude → GitHub 이슈 생성 성공 (#1)
- ✅ Claude → GitHub 파일 Push 성공
- ✅ 대화 내용 문서화 완료
